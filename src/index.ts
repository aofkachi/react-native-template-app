/**
 * @fileoverview App Entry Point
 *
 * This file registers the root component for the React Native app.
 */

import { registerRootComponent } from 'expo';
import App from './App';

// Register the main App component as the root
registerRootComponent(App);

export default App;
